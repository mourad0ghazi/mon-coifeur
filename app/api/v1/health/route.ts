import { NextResponse } from 'next/server';export async function GET(){return NextResponse.json({status:'ok',service:'hlaqti-api',version:'v1',timestamp:new Date().toISOString(),timezone:'Africa/Casablanca'})}
